# 다른 환경에서 SkyWind 1차 구현 이어가기

기준일: 2026-09-09 KST. 이 문서는 **1차 전투 감각 구현과 후속 R2~R6**를 검토하고 다른 환경에서 작업을 이어가기 위한 안내다. 파일명은 기존 인계 링크를 유지한다.

## 1. 현재 상태와 시작 지시

- I01~I07의 45초 사이클·상하 보급/성장·텐션·4패턴·낮밤·바이저 HUD·사건별 빛과 소리, 후속 가속/발광·사운드·보스 전환·흡수 R2~R6를 적용했다. 전체 변경과 최신 검증은 [작업 보고서](docs/WORK_REPORT_20260909.md), R1 당시 증거는 [1차 구현 결과](docs/first-implementation/README.md)로 구분한다.
- 사용자 D42~D48은 구현/보완, D49는 지금까지의 코드·테스트·문서 Git 게시를 승인했다. 이번 Git 묶음에는 최신 R6까지 포함한다. 새 환경에서는 `main`의 실제 HEAD와 작업 보고서를 확인한다. Sites 공개 게임 배포는 별도이며 이번 Git 게시와 함께 실행하지 않는다.
- **아래 지시문은 현재 구현과 증거를 대조하고 후속 개선을 준비하는 요청이다.** I01~I07을 처음부터 다시 만들거나 문서의 오래된 미시작 표현을 현재 상태로 취급하지 않는다.
- 1차 시험값과 한정 범위를 구현하는 것은 전체 메카닉·최종 밸런스·음원 품질을 최종 승인했다는 뜻이 아니다.

### 새 환경에 전달할 지시문

```text
SkyWind의 현재 1차 구현을 검토하고 다음 개선 작업을 준비해 주세요.

루트의 AGENTS.md와 FIRST_IMPLEMENTATION_HANDOFF.md를 읽고,
HANDOFF.md와 docs/WORK_REPORT_20260909.md의 최신 R6 구현·검증을 확인하세요.
docs/first-implementation/README.md와 그 증거는 R1 당시 기록입니다.
작업 HEAD·미커밋 변경·원격 main·실제 소스를 먼저 대조하세요.
시작 HEAD d5c4234507badf0325908fc9545580dc34ba50a0는 구현 전 기준입니다.
최신 Git 변경 묶음의 R1~R6를 과거 공개 게임이나 R1 증거로 덮어쓰지 마세요.

docs/plan/FIRST_IMPLEMENTATION.md는 이미 적용한 I01~I07의 한정 범위와
시험값 기준입니다. 같은 기능을 다시 구현하지 말고 현재 동작과 남은 제한을
검토한 뒤, 사용자가 이번에 요청한 후속 개선에만 필요한 코드와 검증을 수정하세요.

새 Blender 메카닉·약점/장갑 시스템·효과음 제작 앱·외부 오디오 평가기·
최후반 극한 난도는 별도 범위입니다. 현재 입력·작은 핵·플레이어/파일럿·
원본 자산과 다른 작업자의 변경을 보존하고, 실제 실행 주소·검증·남은 범위를
전달해 주세요. 과거 QA ZIP이나 긴 영상을 자동으로 재제작하지 마세요.

이번 실행은 로컬 확인본까지입니다. 커밋·푸시·공개 배포는 별도 요청이 없으면
수행하지 마세요. 이 범위 밖의 실제 사용자 선택이나 환경 장애가 발생하면
근거와 영향 범위를 제시해 주세요.
```

저장소가 없는 환경에서는 먼저 [저장소](https://github.com/Kexxrix/skywind)를 작업할 위치에 받는다. 기존 체크아웃이 있으면 그 위치를 확인하고 사용자 변경을 보존한다. 옛 PC의 절대 경로를 새 환경에 그대로 만들 필요는 없다.

## 2. 저장소와 문서 읽기 순서

| 순서 | 문서 | 목적 |
|---:|---|---|
| 1 | [AGENTS.md](AGENTS.md), [현재 구현](HANDOFF.md) | 현재 작업 규칙·기준 코드·과거 검증과 새 목표 구분 |
| 2 | [작업 보고서](docs/WORK_REPORT_20260909.md), [R1 당시 구현 결과](docs/first-implementation/README.md), [첫 구현 항목](docs/plan/FIRST_IMPLEMENTATION.md) | 최신 R6와 R1 역사 증거, I01~I07 시험 기준·보류 범위 구분 |
| 3 | [결정 기록](docs/plan/DECISIONS.md) | 최신 사용자 방향, 1차/전체 승인 범위와 미결 사항 |
| 4 | [레벨 설계](docs/plan/LEVEL_DESIGN.md), [탄막·텐션](docs/plan/BARRAGE_DESIGN.md) | 상태·무기/보급·피격/근접·각 탄막의 정확한 규칙 |
| 5 | [구현·HUD 설계](docs/plan/IMPLEMENTATION.md), [빛·소리](docs/plan/AUDIO_VISUAL.md) | 낮밤/속도·바이저 HUD·사건별 시청각 연결 |
| 6 | [검증 기준](docs/plan/QUALITY.md) | 해당 검사의 방법과 증거. 1차 제외 항목은 FIRST_IMPLEMENTATION 8절 |

한정 범위·시험 선택은 FIRST_IMPLEMENTATION이 전체 초안의 후보 목록보다 우선한다. 개별 수식·경계 규칙은 그 문서가 연결한 LEVEL_DESIGN/BARRAGE_DESIGN을 따른다. 사용자 후속 지시가 있으면 해당 범위와 검증을 함께 갱신한다.

전체 아트·오디오 도구의 방향은 [메카닉](docs/plan/MECHA_ART.md)·[효과음 도구](docs/plan/SFX_WORKBENCH.md)에 남아 있다. 1차 구현의 필수 도구 설치 목록이 아니다.

## 3. 환경 준비와 기준 확인

새 작업 폴더에 받는 경우:

```sh
git clone https://github.com/Kexxrix/skywind.git
cd skywind
git status --short --branch
git log -1 --oneline
node --version
```

기존 체크아웃이면 `git status --short --branch`로 변경부터 확인한다. 깨끗하고 원격보다 단순히 뒤처졌을 때만 동기화한다. 사용자 변경이 있으면 reset/강제 덮어쓰기하지 않는다.

- 브랜치 기준은 `main`, 1차 구현 시작 HEAD는 `d5c4234507badf0325908fc9545580dc34ba50a0`다. 이번 변경 묶음과 작업 보고서를 포함하는 커밋을 사용하며 원격 `main`과 실제 Git 기록으로 대조한다. 과거 커밋으로 되돌리라는 지시가 아니다.
- 이전 환경의 Node는 `v24.14.0`이었다. 새 환경에서도 Node와 npm이 필요하며 아래 검사로 실제 실행 가능 여부를 확인한다.
- 현재 `package.json`에는 외부 npm 의존성이 없다. 최초 설치를 위한 `npm install`은 필요 없다. 새 의존성·빌드 시스템을 추가하지 않는다.
- 실제 화면에는 WebGL 2와 하드웨어 가속을 지원하는 브라우저가 필요하다. GPU/브라우저를 사용할 수 없는 환경이면 자동 검사와 실제 플레이의 검증 가능 범위를 구분한다.
- 첫 구현에는 Python·Blender·FFmpeg·외부 오디오 API 키가 필요하지 않다. 과거 PC의 설치·하드웨어·열린 Blender 장면을 새 환경의 상태로 간주하지 않는다.

코드 변경 전에는 기존 테스트와 구문 검사를 한 번 실행해 출발점을 기록한다.

```sh
npm test
npm run check
```

시작 기준은 테스트100개였고 이번 구현에 추가 검사가 있다. 최신 수치는 [작업 보고서](docs/WORK_REPORT_20260909.md)를 확인하고 새 환경에서 다시 실행한 결과와 구분한다. PowerShell에서 npm 스크립트 실행 정책 문제가 생기면 `npm.cmd test`, `npm.cmd run check`, `npm.cmd start`를 사용한다.

## 4. 로컬 화면과 빌드 확인

```sh
npm start
```

기본 접속 주소는 [http://127.0.0.1:5173/](http://127.0.0.1:5173/)이다. 현재 서버는 루프백 주소에 바인딩한다. 다른 기기에서 이 주소로 접속할 수 있다고 가정하지 않는다. `file://`로 index.html을 열지 않는다.

5173이 사용 중이면 해당 프로세스를 임의로 종료하지 않는다. 빈 포트를 선택해 설정한다. 예시는 빈 포트 확인 후 사용하는 값이다.

PowerShell:

```powershell
$env:PORT = '5174'
npm.cmd start
```

macOS/Linux 셸:

```sh
PORT=5174 npm start
```

브라우저는 실제 선택한 포트로 연다. 원격 작업 환경이면 그 환경이 제공하는 미리보기/포트 연결을 사용하고 실제 접근 주소를 전달한다. 새로운 공개 터널·사이트를 임의 생성하지 않는다.

첫 화면에서 로딩·기체/파일럿·입력·음소거·정지를 확인한다. 후속 코드를 변경하면 관련 테스트, `npm run check`, `npm run build:site`와 실제 브라우저 검증을 수행한다. 정적 빌드 생성은 공개 배포가 아니다. 실행 방법과 산출물의 범위는 [README](README.md)에 있다.

`build:site`는 허용된 실행 파일만 dist에 복사하고 사운드 provenance/해시를 확인한다. 누락된 자산이나 해시 불일치는 원인을 확인해 해결하며 검사 자체를 약화하지 않는다. 새 모듈을 추가했다면 구문 검사와 빌드 실행 목록도 함께 연결한다.

## 5. 휴대 가능한 참고 자료와 자산

상하 예고의 사용자 참고 이미지를 저장소에 포함했다.

- [바이저 HUD 참고 이미지](docs/plan/references/visor-hud-reference.png): 위/아래 호와 방향 표식, 다른 발광색의 느낌을 참고한다. 정확한 복제 지시가 아니다.
- 원본 첨부 파일과 동일한 PNG 사본이며 변경·재압축하지 않았다. SHA-256: `5bf15a6dd8571bd5ff39059ee0ce05413bdbd00529684b5d80ba4d48bf0f5ab3`.
- 첨부 이미지의 청색 바탕은 게임을 덮는 패널이 아니다. 실제 화면을 투명하게 남기는 얇은 상하 호·라벨 설계는 IMPLEMENTATION의 P01을 따른다.
- 이 파일은 문서용 참고 자료다. 게임이 읽는 자산이나 dist의 필수 파일로 등록하지 않는다.

첫 확인본은 저장소의 현재 SV-01·파일럿·일반 적·보스·효과음·음악을 사용한다. 실행 경로는 현 소스와 매니페스트가 기준이다. 옛 PC의 `.work`, Temp, 로컬 첨부 경로, SampleGame.mp4, 미확보된 SV-01 `.blend`에 의존하는 작업을 만들지 않는다.

식신의 성 2 조사의 공식 출처는 BARRAGE_DESIGN에 링크되어 있다. 조사 때 확인한 외부 GIF의 로컬 사본은 첫 구현의 필수 파일이 아니다. 필요하면 문서의 출처에서 범위를 확인하되 원작 자산 추출·전체 최종 스테이지 분석 완료를 전제하지 않는다.

## 6. 반드시 유지할 첫 범위와 인계 결과

- 첫 WARDEN과 두 번째 CARRIER, 첫/두 번째 패턴 값에 집중한다. 이후 두 번째 난도에서 두 보스를 번갈아 반복하는 확인본이며 최종 극한 난도 완성이 아니다.
- 상하 보급은 실제 고도 이동으로 획득한다. 유지 보급의 기본 성장/특수 연장을 포함하고, 양쪽 획득은 정상 입력에서 어렵지만 가능한지 검사한다.
- 작은 핵 1.85·텐션 2초를 유지한다. 근접 거리·피해 1.5배·갱신 규칙·3속도와 패턴 시험값은 FIRST_IMPLEMENTATION이 선택한 상세안을 적용한다.
- HUD 색은 방향에 고정하고 보급 종류가 상하로 바뀌어도 방향색을 바꾸지 않는다. 텐션 탄·적탄·파일럿·보스 게이지·터치 입력과 구분한다.
- 효과음의 기존 샘플/짧은 합성음을 사용해 정상과 텐션의 차이를 연결한다. 기술적 출력 성공을 최종 청음 품질 승인으로 바꾸지 않는다.
- 게임 코드 구현·첫 범위 검증·사용자 체감 승인·Git 게시·Sites 배포를 별도 상태로 보고한다.

최종 인계에는 실제 시작/종료 소스 기준, 변경 파일, I01~I07 구현 여부, 조정한 시험값과 이유, 수행한 명령 결과, 실제 브라우저/입력/프레임 관측, 실행 주소, 미수행·후속 범위를 포함한다. 현재 작업 환경에서 불가능한 검사는 이유와 가능한 대체 관측을 명시한다.

후속 변경으로 상태가 달라지면 HANDOFF와 해당 결과 기록을 갱신한다. 1차 밖의 작업을 자동 시작하거나 설계 문서 작성 당시의 과거 측정을 새 결과로 재사용하지 않는다.
