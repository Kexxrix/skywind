# SkyWind 작업 인계 규칙

새 작업은 `README.md`, `HANDOFF.md`, `docs/patch2/STATUS.md`를 먼저 읽고 현재 소스와 Git 상태를 확인한다. 이 저장소는 기존 Canvas 2D + WebGL 2 게임의 실제 작업본이다. 과거 공개 사이트나 QA 기준본으로 최신 소스를 덮어쓰지 않는다.

## 범위와 보존

- 요청한 부분만 최소 수정한다. 엔진·의존성·구조를 임의로 교체하지 않는다.
- PATCH2의 중앙 핵 반경은 `PLAYER_HIT_RADIUS = 1.85`이다. 화면의 선명한 핵과 피해 판정이 같은 값을 사용한다. 날개·광번짐·피격 링은 제외하며 아이템 획득 여유는 별도 유지한다.
- 적 수·탄속·패턴·HP·피해량, 무기 성능·간격·지속시간·교체·드롭, 이동 범위·기체 크기·지상 디자인·음악은 새 지시 없이 변경하지 않는다.
- 지정 무기 교체·시간 연장·고도별 보급 예고·흡수 획득 등 새 아이템 시스템은 **보류**다. PATCH2 검토 전에 연달아 적용하지 않는다.
- 원본 자산을 보존하고 편집본과 구분한다. 기존 보고서와 스크린샷을 새 결과처럼 덮어쓰지 않는다.

## 검증 시간과 보고

- 사용자는 장시간 녹화 작업을 중단시켰다. 앞으로 기본 검증은 **자동 테스트 + 실제 스크린샷 + 짧은 상태 기록**이다. 영상은 기본적으로 만들지 않는다.
- 영상이 꼭 필요하고 요청 범위에 맞는 경우에만 5~10초 정도의 한정된 장면을 사용한다. 녹화·인코딩·저장이 지연되면 중단하고 원인을 보고한다. 수시간 대기나 무한 재시도는 금지한다.
- 기존 명령 `npm test`, `npm run check`를 우선한다. 배포 준비 시 `npm run build:site`를 실행한다. 변경 범위에 맞는 검증이 통과하면 이유 없이 반복하거나 확대하지 않는다.
- 직접 확인하지 않은 항목은 미검증으로 쓴다. 오디오 노드/파형 검사는 실제 청음 승인이 아니다. 원본 영상과 완전히 같다는 주장을 하지 않는다.
- 진행 중 60초 이상 설명 없이 방치하지 않는다. 마무리에 변경 파일·실제 검증·남은 제한을 알린다.

## Git, 사운드 권한, 공개 사이트

- 작업 시작 시 `git status --short --branch`, `git fetch origin`으로 비교한다. 미커밋 변경을 보존하며 깨끗하고 뒤처진 경우에만 `git pull --ff-only`한다. 충돌 시 reset/force push로 덮어쓰지 않는다.
- 커밋·푸시·배포는 해당 사용자 요청의 범위에서 수행한다. 2026-09-07 요청은 이 GitHub 최초 백업을 승인했다. 이후 작업의 무조건적인 공개 승인은 아니다.
- 사용자가 2026-09-07 제공 사운드의 공개 사용 권한을 확인하고 공개 저장소 전체 백업을 승인했다. 기록은 `assets/audio/sfx-patch1/provenance.json` 및 `docs/RIGHTS_STATUS.md`에 있다. 별도의 라이선스 실사나 직접 청음 완료로 확대 해석하지 않는다.
- `.openai/hosting.json`은 기존 Sites 식별자만 포함한다. 그대로 유지한다. GitHub에 push하는 것과 Sites 배포는 별개다. 새 사이트를 임의 생성하거나 기존 공개본을 자동 배포하지 않는다.
- 비밀정보·인증 토큰·.env·기계별 경로 설정·.work·임시 터널·대용량 원시 녹화는 커밋하지 않는다. `assets/`의 제작 원본은 백업 대상이다.
