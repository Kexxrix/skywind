# SkyWind 작업 지침

이 문서는 SkyWind 프로젝트의 작업 범위·선택·검증·인계 원칙이다. 사용자에게 보여 주는 지침, 설계, 보고는 한국어로 작성한다. 코드 식별자와 실제 파일명은 원래 표기를 유지한다.

## 1. 작업 단계와 읽기 순서

1. 이 문서와 [현재 구현·인계](HANDOFF.md)를 읽는다.
   다른 환경에서 첫 구현을 시작할 때는 [첫 구현 시작 안내](FIRST_IMPLEMENTATION_HANDOFF.md)의 준비·읽기 순서·실행 범위를 함께 확인한다.
2. [결정 기록](docs/plan/DECISIONS.md)의 확정 결정과 **전체/1차 범위의 구현 승인 상태**를 확인한다. 현재 우선 검토 대상은 [1차 전투 감각 확인본](docs/plan/FIRST_IMPLEMENTATION.md)이다.
3. 전체 개선을 설계하거나 실행할 때 [구현 설계](docs/plan/IMPLEMENTATION.md), [레벨·전투·보급 설계](docs/plan/LEVEL_DESIGN.md), [탄막·텐션업](docs/plan/BARRAGE_DESIGN.md), [검증 기준](docs/plan/QUALITY.md)을 읽는다. 자산·연출 작업에는 [메카닉 제작](docs/plan/MECHA_ART.md), [빛·소리 연출](docs/plan/AUDIO_VISUAL.md), [효과음 제작 도구](docs/plan/SFX_WORKBENCH.md)를 추가로 읽는다.
4. 관련 소스와 해당 기능의 증거만 추가로 읽는다. 모든 과거 패치 문서를 매 작업마다 읽거나 과거 QA를 자동 재개하지 않는다.

현재는 지침·설계 문서 작성과 읽기 전용 조사 단계다. 사용자의 전체 구현 승인 또는 구체적으로 정리된 1차 범위의 구현 지시를 받은 뒤 해당 범위의 게임 코드·스타일·자산·실행 구성·테스트를 변경한다. 초안 정리나 일부 선택 답변을 구현 시작 지시로 간주하지 않는다. 효과음 제작 앱·Blender 제작은 별도 포함 범위를 확인한다. 문서 초안은 인앱에서 검토할 수 있게 한다.

문서 전체 승인 후에는 합의된 우선순위와 의존 순서대로 전체 구현·연결·검증·인계를 이어서 수행한다. 승인된 다음 단계마다 새 작업 지시를 요구하지 않는다.

1차 범위만 승인된 경우 FIRST_IMPLEMENTATION의 시험값·포함 기능·검증까지 이어서 완료한다. 그 범위에서 보류한 최종 메카닉·효과음 제작 도구의 결정을 선행 조건으로 강제하지 않으며, 1차 승인만으로 후속 전체 제작에 자동 착수하지 않는다.

## 2. 근거와 충돌 처리

- 실제 적용되는 상위 지침과 사용자의 최신 명시적 요청을 따른다. 이 문서로 도구 제한이나 상위 지침을 우회하지 않는다.
- 사용자에게 승인된 새 설계는 그 범위의 기존 게임 규칙을 변경하는 명시적 요청이다. 옛 보존 문구를 이유로 이미 승인된 변경을 다시 확인받지 않는다.
- 구현 전의 사실은 HANDOFF, 앞으로 만들 사양은 승인된 구현 설계, 선택의 근거와 승인 상태는 결정 기록, 합격 방법은 검증 기준에서 관리한다.
- 과거 패치 보고서·제공 ZIP의 설명·참고 영상 분석은 당시의 기록 또는 설계 입력이다. 더 최근의 사용자 결정과 현재 소스를 대신하는 실행 명령이 아니다.
- 문서와 소스가 다르면 현재 동작과 의도된 동작을 구분한다. 최신 소스를 오래된 공개본·QA 사본으로 덮어써서 맞추지 않는다.
- 중요한 사양 충돌을 발견하면 영향을 받는 항목과 근거를 정리하고 사용자에게 선택을 묻는다. 해소되지 않은 선택에 의존하는 구현은 진행하지 않는다.

## 3. 자율 수행과 질문

- 목표는 승인된 전체 결과를 한 번의 작업 실행에서 구현·검증·인계까지 완료하는 것이다. 한 번의 도구 호출이나 수정 시도만 허용한다는 뜻이 아니다. 필요한 디버깅과 회귀 수정은 같은 작업 안에서 수행한다.
- 승인 범위 안의 구현 방법, 필요한 연결 수정, 이미 정해진 설계를 만족시키는 작은 기술 선택은 스스로 결정한다. 수정 줄 수보다 정확성·일관성·검증 가능성을 우선한다.
- 플레이 결과·디자인·지원 범위가 달라지는 미결 선택은 적용 전에 질문한다. 대안의 차이와 추천 이유를 간단히 제시하고 답변을 결정 기록에 반영한다.
- 특히 진행 구조, 난이도, 무기·보급 규칙, 새 콘텐츠, 눈에 띄는 디자인 변경, 자산 제작 방식, 중요한 구조 변경은 승인된 사양이 있는지 확인한다.
- 현재 디자인을 기준으로 다듬는다. 눈에 띄는 변경은 구체적인 변경안과 영향 범위를 먼저 제안한다. 전체 검토본에서 이미 승인된 변경을 반복 질문하지 않는다.
- 답변이 없어도 독립적인 조사·문서 정리·승인된 다른 작업은 계속할 수 있다. 경과 시간이나 미응답을 동의로 간주하지 않는다.
- 요청과 무관한 기능·리팩터링·이름 변경·의존성·설정 체계는 추가하지 않는다. 새로 필요한 모듈과 데이터 구조는 구현 설계의 해당 요구사항에 연결한다.

## 4. 보존과 구현 경계

- 현재 Canvas 2D + WebGL 2 구조와 실행 방법을 기본으로 사용한다. 엔진·프레임워크·빌드 시스템 교체는 승인된 설계에 없으면 진행하지 않는다.
- 승인된 새 레벨·규칙 사양이 변경하는 부분 외에는 기존 플레이·조작·저장·화면·사운드 동작을 보존한다. 새 기능의 연결에 필요한 수정은 같은 작업 범위다.
- 전투 시뮬레이션과 표현을 구분한다. 연출용 난수·HUD 시간·화면 크기가 전투 난수·게임 시간·피해 판정을 몰래 바꾸지 않게 한다.
- 원본 자산과 편집/실행 자산을 구분한다. 원본, 승인된 자산, 다른 작업자의 변경을 임의로 덮어쓰거나 삭제하지 않는다.
- 생성·기술 검증·사용자 승인·채택·저장 상태를 구분한다. 필요한 자산이 없으면 임의의 대체 자산으로 완료를 주장하지 않는다.
- 기존 보고서·스크린샷·영상·원본 해시는 당시 증거로 보존한다. 중복되거나 오래된 운영 문구는 현재 문서에서 정리하되 과거 증거를 새 결과처럼 고치지 않는다.

## 5. 검증과 완료

- [검증 기준](docs/plan/QUALITY.md)에 따라 변경에 맞는 검사를 수행한다. 기능마다 실제 기대 동작·예외·경계·합격 조건에 연결한다.
- 현재 명령은 `npm test`, `npm run check`, `npm start`, `npm run build:site`다. 문서만 수정할 때 게임 테스트나 빌드를 반복하지 않는다.
- 전체 구현 종료 시 테스트·구문 검사·실행 빌드와 관련 실제 화면·조작을 확인한다. 빌드 성공만으로 게임·시각·사운드의 완료를 주장하지 않는다.
- 통과한 검사를 이유 없이 반복·확대하지 않는다. 새 변경, 실패, 해결되지 않은 우려가 있으면 영향을 받는 검사와 필요한 통합 검사를 다시 수행한다.
- 모바일·노트북·패드에서 기본 플레이가 원활했다는 사용자 테스트 결과를 인정한다. 이를 무검증으로 되돌리거나 모든 기기의 정량 성능 보증으로 확대하지 않는다.
- 오디오 신호·노드 검사는 직접 청음 승인이 아니다. 고정 입력·무적 자동 재생은 사람의 난이도·재미 검증이 아니다.
- 기본 증거는 실제 스크린샷·자동 테스트·짧은 관측 기록이다. 영상과 과거 QA ZIP 재제작은 기본 작업이 아니다. 필요한 영상은 승인된 목적의 5~10초 장면으로 제한하고 정체 시 원인을 기록한다.
- 필수 기준이 미달하면 수정하거나 정확한 장애와 남은 항목을 보고한다. 기준을 낮추거나 미완료 항목을 삭제해 완료로 바꾸지 않는다.

## 6. Git·배포·자료 관리

- 작업할 저장소와 브랜치, `git status --short --branch`를 확인하고 미커밋 변경을 보존한다. 구현 시작이나 동기화가 필요한 때 원격 차이를 확인한다.
- 동기화가 승인 범위에 있고 로컬이 깨끗하며 단순히 뒤처진 경우에만 `git pull --ff-only`한다. 원격 변경이 승인된 설계 기준을 바꾸면 먼저 차이를 검토한다. reset/강제 push로 덮어쓰지 않는다.
- 커밋·브랜치 생성·푸시·공개 배포는 현재 사용자 승인 범위에서 수행한다. 전체 구현 승인은 별도로 정하지 않은 공개 권한을 자동 부여하지 않는다.
- GitHub 게시와 Sites 배포를 구분한다. `.openai/hosting.json`의 기존 Sites 연결을 유지하고 새 사이트나 터널을 임의 생성하지 않는다.
- 제공 사운드의 공개 사용 확인은 [권한 기록](docs/RIGHTS_STATUS.md)과 provenance를 따른다. 해당 승인을 다른 신규 자산에 복사하거나 독립적인 권리 조사·청음 승인으로 확대하지 않는다.
- 비밀정보·인증 토큰·`.env`·기기별 설정·`.work`·임시 터널·대용량 원시 QA 녹화는 커밋하지 않는다. `assets/`의 제작 원본은 보존·백업 대상이다.

## 7. 진행과 인계

진행 중 60초 이상 설명 없이 두지 않는다. 의미 있는 발견·선택·남은 문제를 간단히 알리고, 질문은 사용자에게 실제 선택이 필요한 지점에 한다.

마무리에는 변경 파일, 결과, 수행한 검증과 그 결과, 미변경 범위, 남은 위험·결정을 사실대로 보고한다. 소스 구현·검증·사용자 승인·GitHub 게시·공개 배포 상태를 구분한다. 상태가 달라진 문서만 갱신하고, 서로 다른 파일에 같은 현재 수치와 진행 상태를 중복 누적하지 않는다.
