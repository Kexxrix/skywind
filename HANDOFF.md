# SkyWind 현재 구현과 인계

기준일: 2026-09-09 KST. 이 문서는 **현재 구현된 기준본**을 설명한다. 새로 만들 기능의 사양은 [전체 개선 설계](docs/plan/IMPLEMENTATION.md), 사용자 결정과 구현 허용 상태는 [결정 기록](docs/plan/DECISIONS.md)을 따른다.

## 현재 작업 단계

사용자는 게임 레벨 디자인 전체를 최우선으로 정하고, 난이도·무기·아이템 규칙과 적·보스·UI·화면·연출·사운드·조작감까지 전체 개선을 상세 설계한 뒤 한 번의 작업 실행으로 구현하기로 했다. 새 지역은 이후로 미룬다. 현재 플레이어·파일럿을 기준으로 건담 00 오마주의 메카닉을 제작하고, 빛·소리는 음악 직접 연계 없이 전투 사건에 반응하게 만드는 방향을 확정했다. 세부 제안과 승인 상태는 설계 문서에서 관리한다.

**현재는 I01~I07과 후속 R2~R6 코드 구현 및 기록된 범위의 검증을 마친 단계다.** 사용자가 “구현 안내에 따라 구현 진행. 질문이 필요한 부분은 물어보지 않고 우선판단으로 구현”을 지시해 [1차 전투 감각 확인본](docs/plan/FIRST_IMPLEMENTATION.md)의 시험값과 기존 자산 재사용을 적용하고 이후 피드백을 보완했다. 최신 검사·남은 제한은 [작업 보고서](docs/WORK_REPORT_20260909.md), R1 당시 스크린샷과 관측은 [1차 구현 결과](docs/first-implementation/README.md)를 따른다. 최종 밸런스·청음의 사용자 승인은 별도이며, 새 작업은 [AGENTS.md](AGENTS.md)부터 읽는다.

2026-09-09 D49로 지금까지의 게임 코드·테스트·문서를 GitHub에 함께 게시하는 작업을 승인받았다. 이번 Git 묶음은 I01~I07과 R2~R6를 포함하며 [작업 보고서](docs/WORK_REPORT_20260909.md)와 [다른 환경 안내](FIRST_IMPLEMENTATION_HANDOFF.md)를 따른다. 아래 패치별 게시 미수행 문구는 각 작업 당시 기록이다. Sites 재배포는 이번 요청에 포함하지 않았고 과거 PC의 Temp 경로는 실행에 필요하지 않다.

## 기준 소스와 확인 범위

### 상하 보급 흡수·이동 가시성 수정 R6 — 2026-09-09

- D48의 실제 원인: 중앙 회복과 달리 상하 보급은 접근 예고가 끝날 때까지 근접 흡수를 거부했다. 정상 하강→5.25초부터 상승하는 재현에서 하단 보급을78.57 거리로 스쳐도 놓쳤다. 계속 하강하면48 거리로 붙어도 기다리다가6초부터 흡수했고, 이전 감속 곡선/거리12 조기 획득으로 약0.033초 만에 사라졌다. 이전 R5의 활성 아이템 배치 검사는 이 실제 예약 보급 경로를 놓쳤다.
- 이제 실제 접근 중인 상하 보급도 회복과 같은 범위에 들어오면 즉시 흡수를 시작한다. 예고 종료 시각까지 기다리지 않는다. 기본 반경212·정후방318, 만료 후 신규 흡수 금지와 잡힌 아이템의 완료, 단일 획득은 유지한다. 기존 접근 경로·공급 시계6/15초·만료 시각은 유지하지만, 실제 획득은 경로상 근접 시 더 일찍 가능하다.
- 흡수는 일정한 속도로 최대0.18초 이동하고, 이동 거리가 있는 아이템은 최소0.14초 표시한다. 이미 중심12 안에서 접촉한 아이템의 즉시 획득은 유지한다. 아이콘은 이동의65%까지 원래 크기를 유지하고 끝부분에서만 줄어든다. 회복/보급 모두 실제 이동 경로 뒤로 최대120 길이의 빛 꼬리를 그리며 전경 앞에서 표시한다. 이 값은 R5의0.10초 설정을 대체한다.
- 검증: 전체164개 테스트·구문 검사·정적 빌드 통과(103파일/29.73MiB). 실제 예약 보급의 양쪽 접근/조기 획득/기존 도착 시각 후 획득 상태 유지, 하강→상승의 기존 누락 경로를30/60/144Hz에서 검사했다. 근거리 아이템의 최소 표시와20Hz를 포함한 이동/단일 획득, 경로를 따르는 꼬리와 중간 아이콘 크기도 확인했다.
- 인앱 실제 게임 렌더에서 시드71·1/120초 정상 방향/발사 입력 재현을 사용했다. 아이템 생성/위치·무적을 지정하지 않았다. 하단 변경 보급은4.9667초부터 흡수해5.1283초 LANCE, 상단 유지 보급은5.2333초부터 흡수해5.3950초 기본 Lv.2를 획득했다. 중간0.0783초의 아이콘·이동 경로를 각각 화면으로 확인했다. 이는 정상 속도 수동 플레이의 체감 승인이나 실기 FPS 보증을 대신하지 않는다. 영상은 만들지 않았다.
- 수정 파일: `src/game.js`, `src/renderer.js`, `tests/pickup-attraction.test.js`, `tests/first-combat.test.js`, `tests/first-visuals.test.js`, `HANDOFF.md`, `docs/plan/DECISIONS.md`. 피해 핵·무기 효과/성능·음악/사운드·HUD 배치는 유지했다. 로컬 적용이며 Git 게시·공개 배포는 하지 않았다.

### 흡수 범위·속도 보완 R5 — 2026-09-08

- D47에 따라 기본 흡수 반경106→212(모든 방향2배), 후방은 화면 왼쪽으로106만큼 옮긴 같은 크기의 원을 추가해 정후방318까지 잡는다. 두 영역은 겹치며 전방·상하를 줄이지 않는다. 프레임 시작/끝의 카메라 회전을 반영한 연속 거리 검사로 후방 대각선과 상대 이동을 처리한다.
- 최대 흡수 시간0.26→0.10초. 느린 출발의 가속 곡선을 즉시 당기는 감속 곡선으로 바꿨다. 기존 중심12 이내의 단일 획득·도착/만료 규칙·피해 핵1.85·무기/보급 효과·사운드는 유지했다. 조정값은 `PICKUP_ATTRACTION`에 있다. R4의 흡수 범위/시간보다 이 값이 우선한다.
- 검증: 전체161개 테스트·구문 검사·정적 빌드 통과. 전방·상하·후방·후방 대각선의 안/밖, 세 고도,30/60/144Hz의 빠른 흡수와 단일 획득을 검사했다. 기존 화면 이탈 테스트는 확대 범위에서 자동 획득되지 않도록 기체 위치만 떨어뜨려 원래의 이탈 검사를 유지했다.
- 인앱의 실제 렌더에서 아이템만 화면 후방300/아래60 위치로 시험 배치했다.0.025초에 절반 이상 끌려온 화면과 약0.067초 내 기본 Lv.2 단일 획득을 확인했다. 짧은 고정 스텝 통제 장면이며 실제 기기의 실시간 체감 승인은 별도다. 영상은 만들지 않았다. 임시 검사 화면을 제거하고 기존 게임 탭을 새로고침했다.
- 수정 파일: `src/game.js`, `tests/pickup-attraction.test.js`, `tests/player-hitpoint.test.js`, `tests/difficulty.test.js`, `HANDOFF.md`, `docs/plan/DECISIONS.md`. 로컬 적용이며 커밋·공개 배포는 하지 않았다.

### 흡수·사운드·보스 전환 보완 R4 — 2026-09-08

사용자 D45~D46에 따라 R3의 접근 경로를 보존하면서 근접 흡수와 사건별 사운드를 보완하고, 보스 등장 직후 아래로 뛰는 오류를 수정했다.

- 흡수: 기존 중심 간 획득 거리53에서 흡수 시작 반경106으로 확대했다. 지름212는 기체 표시 폭106의 2배다. 회복·드론·변경·유지 등 모든 아이템이 붙잡힌 지점부터 현재 기체 위치까지 최대0.26초 동안 가속해 들어오며 작아진다. 중심12 이내에서 기존 효과를 한 번 적용한다. 흡수 중에는 전경 앞에 표시하고 보급의 빛 꼬리도 이동 방향에 맞춘다. 플레이어 피해 핵1.85는 그대로다.
- 보급 도착 전 흡수는 금지한다. 기존2.1초 획득 창은 흡수 시작 가능 시간이며, 시간 안에 잡은 아이템은 창이 끝나도 짧은 흡수를 마친다. 기체 이동·카메라 추적, 보급 종류/일정·무기 성능/지속시간·효과는 유지한다.
- 텐션: 기존 약한 단일 시작음을 상승음·저음·지연된 고음의3층으로 바꾸고 일반 전투 공명보다 우선 재생한다. 갱신음은0.24초 간격으로 묶으며 자연 만료음과 피격 해제를 구분한다. 시작 순간 발사음은38%, 음악은58%로 잠깐 낮춰 사건음을 드러내고 복구한다. 곡·재생 위치·상태별 교차 전환은 유지한다.
- 전체 효과음: 주무기 발사 레벨을 높이고 드론 비중을 줄였다. 무기 명중음은 강화하고 재질음은 낮추며 폭발 꼬리를 짧게 해 연속 타격의 구분을 개선하는 방향으로 조정했다. 텐션 발사 공명·체인 단계음·획득음도 강화하고 흡수 시작음을 별도로 연결했다. 고정 플레이어 피격 샘플은 유지하며 피격 시 음악도 잠깐 낮춘다. 기존50개 샘플과 제작 원본은 바꾸지 않았다. 전체 음소거 버튼의 안내를 실제 기능에 맞게 “사운드”로 수정했다.
- 보스: 진입1.2초가 누적된 `enemy.age`를 바로 전투 상하 운동에 사용해 WARDEN은 약130, CARRIER는 약32 게임 단위 아래로 뛰던 오류다. 전투 시작부터 흐르는 `phaseTime`을 사용해 진입 중앙 높이에서 연속 이동한다. 보스 HP·공격 시계·패턴·피해량은 변경하지 않았다.
- 검증: 전체159개 테스트·구문 검사·정적 빌드 통과(103파일/29.72MiB). 모든 아이템의 지연 획득/중복 방지·만료 경계·정지/재시작, 움직이는 기체와 카메라의30/60/144Hz 흡수, 두 보스의 세 고도·세 프레임 조건을 검사했다. 오디오 우선순위·음악 볼륨 복귀/교차 전환·정지/음소거/게임오버 정리도 통과했다.
- 인앱 실제 렌더에서 정상 방향/발사 입력의1/120초 고정 스텝 재현으로 하단 보급의 흡수 시작·중간·LANCE 획득을 확인했다. 보스 등장 시각만 앞당긴 장면에서 상단 WARDEN/하단 CARRIER 전환도 확인했다. 전환 전후0.04초 관측 이동은 각각 약1.91/0.41로 연속적이었다. 무적·플레이어 위치 지정은 사용하지 않았다. 이 검사는 실시간 성능 또는 사람이 느끼는 난이도 검증을 대신하지 않는다.
- 사운드는 브라우저의 기존 음원 로드·출력 신호를 짧게 검사했다. 직접 청음한 음질·최종 믹스의 사용자 승인은 미검증이다. 캡처와 관측 JSON은 이번 PC의 `%TEMP%/SkyWind-R4-QA-20260908/`에 보관하며 실행에 필요하지 않다. 영상 제작·커밋·푸시·공개 배포는 하지 않았다.
- 이번 수정 파일: `src/game.js`, `src/renderer.js`, `src/audio.js`, `src/main.js`, `index.html`; 검증은 `tests/pickup-attraction.test.js`(신규), `tests/player-hitpoint.test.js`, `tests/first-combat.test.js`, `tests/audio.test.js`; 기록은 `HANDOFF.md`, `docs/plan/DECISIONS.md`. 다른 미커밋 변경은 이전 R1~R3 작업이며 보존했다.

### 보급 접근·고도 보완 R3 — 2026-09-08

사용자 D44에 따라 무기·시간 연장 HUD를 보존하고 실제 보급 경로와 가림만 보완했다.

- 기존에는 도착 순간 x330에 생성되어 80px/s로 잠깐 움직였다. 이제 예고가 시작될 때 화면 밖 x1380에서 생성되고 3초간 x360까지 감속하며 접근한다. 같은 아이템이 도착 후 150px/s로 x45까지 지나가므로 기체의 좌우 이동 구간 전체를 통과한다. 접근과 도착 사이에 위치·속도 단절이 없다.
- 고도 경로는 상단 -60 / 하단 780의 비행 좌표에 고정하고 매 갱신에서 현재 카메라 회전으로 투영한다. 기존 최상·최하단과의 간격64px는 획득 반경53px보다 컸다. 현재 간격48px는 기존 획득 반경 안이며, 최고·최저 고도 화면에서는 중심이 y96 / y624에 표시된다. 상단 중심은 파일럿 프레임 하단보다 아래로 두었다.
- 실제 보급은 전경 구름·수목 이후, 위험 탄의 핵 이전에 그린다. 기존 밝은 앰버/민트 링·아이콘에 오른쪽으로 흐르는 짧은 빛 꼬리를 연결했다. 회복·드론 드롭의 기존 깊이는 유지한다.
- 첫 도착6초·주기15초·예고3초·도착 후 획득 창2.1초, 종류 선정·획득 효과·무기 규칙·획득 반경·기체 이동·전투·HUD·음악은 유지했다. 접근 중 먼저 닿아도 도착 전에는 획득하지 않는다.
- 검증: 전체149개 테스트, 구문 검사, 정적 빌드 통과. 30/60/144Hz에서 위/아래 입력만 유지한 극한 고도 획득, 연속 경로, 도착 전 미획득·만료 경계, 사전 배치 시 양쪽 획득을 검사했다. 인앱960×640의 실제 게임 렌더에서 4.5초/5.8초 접근 화면과 양쪽 획득을 확인했다. 최종 화면 검사는 1/120초 고정 스텝의 정상 입력 재현이며 무적·위치 지정은 사용하지 않았다. 상단7.8667초·하단6.0초 획득 기록과 스크린샷은 `.work/supply-approach-r3/verified-*.jpg`, `*-input-record.json`에 있다. 실시간 접근도 별도 확인했으나 이번 검사로 성능을 보증하지 않는다. 영상·Git 게시·공개 배포는 하지 않았다.

### 1차 구현 후 가속·발광 보완 R2 — 2026-09-08

사용자 D43에 따라 밝고 아름다운 발광과 강한 속도감을 우선했다. R1 보고서·캡처는 그 당시 자료로 보존하며 아래는 후속 변경이다.

- 배경 전용 기준 속도1.3→1.7, 일반전 0/15/30/42초 배율1/1.35/1.8/2.2, 보스4.6배. R1 대비 실제 배경 이동량은 초반1.31배·접근1.92배·보스2.01배. 전경 구름 위로 원근 집중선 추가.
- 보급: 검정 패널/그림자 대신 앰버·민트 투명 발광, 호의 흐르는 광선, 실제 아이템의 링·빛 입자. 적탄: 어두운 받침 대신 분홍·흰색 발광 코로나. 기존 C 식별자는 유지하며 표현을 교체했다.
- 텐션: 발동 광파0.32→0.52초·큰 금색 에너지 파장, 기체 주변 금색 아크/추진 입자, 텐션 발사광1.55배. 기존 2초 피해 강화 규칙은 그대로다. HUD는 TENSION UP·밝은 게이지·부드러운 금빛 맥동.
- 게임 규칙·피격 반경·적탄 이동·무기 성능·보급 일정·기체/배경 원본·음악/사운드는 R1과 동일하다. 움직임 줄이기 설정에서는 새 반복 움직임을 줄이거나 정적 빛으로 표시한다.
- 검증: 전체 테스트146/146, 마지막 발광·저감 모션 조정 후 시각 검사13/13·구문·빌드 통과(103파일/29.72MiB). 인앱1280×900과844×390에서 보급/적탄의 밝은 발광, 구름 위 집중선, 텐션 발동·유지 화면을 확인했다. 통제 장면을 사용한 짧은 비교이며 실제 휴대기기·장시간 성능을 새로 검증한 것은 아니다. 원본 비교와 임시 캡처는 로컬 임시 자료로 보존하고 이전 R1 보고서를 덮어쓰지 않았다. 이번에도 커밋·배포·영상 제작은 하지 않았다.

### 소스 기준

- 저장소: <https://github.com/Kexxrix/skywind>, 기본 브랜치 `main`.
- 1차 구현 시작 HEAD: `d5c4234507badf0325908fc9545580dc34ba50a0`. 이번 Git 변경 묶음은 그 위의 I01~I07과 R2~R6를 포함한다. 다른 환경에서는 실제 HEAD·원격 `main`·변경 파일을 먼저 확인한다.
- 이전 공개 실행 코드의 커밋은 `73b165c320343af21b102befb5de0df8d7c9714e`이며 파일럿 HUD FX3와 SV-01 회전/WebP 최적화를 포함한다. 이 과거 코드로 현재 작업본을 덮어쓰지 않는다.
- 시작 기준의 기존 테스트100개는 출발 당시 결과다. 최신 R6의 검사 수치와 관측은 위 R6 및 [작업 보고서](docs/WORK_REPORT_20260909.md)를 따른다. [1차 구현 결과](docs/first-implementation/README.md)의142개와 스크린샷은 R1 당시 증거다.
- 최신 공개 배포 기록은 2026-09-08 02:34 KST의 **Sites 버전 5**다. 당시 실행 파일 101개 검증 결과는 [공개 배포 기록](docs/PUBLIC_UPDATE_PILOT_20260908.md)에 있다. 이번 1차 구현은 공개 사이트에 배포하지 않았다.

## 실제 이용자 피드백

2026-09-08 사용자는 모바일 실기와 노트북·패드 환경에서 다른 이용자들에게 플레이를 시켰으며, **플레이 자체에는 문제가 없었다는 피드백을 받았다**고 밝혔다. **UI 크기 대응은 남은 문제**로 지적했다.

이는 이전 실행본에 대한 사용자 테스트 확인이다. 기기 모델·OS·브라우저·FPS·플레이 시간은 제공되지 않았다. 과거 문서의 '모바일 실기 미검증'은 당시 에이전트의 검증 범위를 뜻하며, 사용자 피드백을 무효화하지 않는다. 이 피드백을 전 기기 성능 보증이나 이번 1차 구현본의 실기 검증으로 확대하지 않는다.

## 현재 게임 구현

아래는 이번 로컬 1차 구현의 동작이다. 후속 변경 수치와 검증은 위 R2~R6를 우선하며, 이후 최종 설계 전체의 구현 상태와 구분한다.

| 영역 | 현재 동작 | 주요 근거 |
|---|---|---|
| 진행 | 타이틀 → 기체 진입 → 일반전 45초 → 보스 진입 1.2초 → 실제 격파 후 다음 일반전 45초. 42초 사전 예고와 새 일반 공격 중지, HP 소진 시 게임오버·재도전 | `src/game.js`, `src/main.js`, `src/level.js` |
| 점수 | 적 격파, 연속 격파 배수, 아이템 획득 점수, 최고 점수 저장 | `destroyEnemy`, `updatePickups`, `finish` |
| HP | 시작·최대 100, 회복 +30. 피격 후 무적 구간. HP 소진 시 게임오버 | `createGame`, `damagePlayer` |
| 피격 | 중앙 핵 반경 1.85와 실제 피해 판정 공유. 날개·광번짐 제외. 고속 상대 이동 검사 | `PLAYER_HIT_RADIUS`, `tests/player-hitpoint.test.js` |
| 비행 | 화면 왼쪽 약 1/3에서 이동. 화면 이동과 고도·카메라 추적 연결 | `worldToScreen`, `screenToWorld`, `constrainPlayer` |
| 입력 | 키보드 WASD/방향키, Space/J 발사, 마우스 이동·발사, 터치 조이스틱·SHOT | `src/main.js` |
| 상태 | P/Esc 일시정지, 창 이탈 시 자동 정지, M 음소거, F 전체 화면, 로딩 오류 안내 | `src/main.js`, `src/boot.js` |
| 기체 | SV-01 R4 21프레임의 상승·중립·하강, 기수 회전 ±6°, 발사 위치 연계 | `src/renderer.js`, `playerHeading`, `playerMuzzle` |
| 기본 무기 | Lv.1~5. 유지 보급으로 성장, 2줄→3줄→4줄 및 피해 증가. 특수·피격·보스 전환에서 성장 보존, 재도전에서 초기화 | `basicWeapon`, `firePlayer`, `applyPickup` |
| 강화 무기 | SPREAD 5방향 / LANCE 관통 3줄 / HELIX 진동 4줄. 예고 때 현재 특수 제외 후보 선택, 획득 시 18초·유지 +15초·최대 45초. 만료 시 성장한 기본 무기로 복귀 | `WEAPON_DURATION`, `WEAPON_MAX_DURATION`, `applyPickup` |
| 드론 | 위·아래 보조 드론이 발사 입력과 함께 공격. 주무기와 별도로 15초 | `DRONE_DURATION`, `updatePlayer` |
| 일반 적 | 기존 적 자산·행동 정의 보존. 첫 확인본 편대는 beetle/dragonfly와 패턴 담당 orb/claw/worm/ray를 사용 | `spawnWave`, `ENEMY_SPECS` |
| 보스 | WARDEN→IRON CARRIER 반복. 첫 WARDEN HP 420, 두 번째 CARRIER 567. 실제 격파 시 적탄·호위 정리와 회복 +30 아이템 1개. 호위는 점수·드롭 없음 | `BOSS_KINDS`, `spawnBoss`, `destroyEnemy` |
| 난이도·탄막 | 첫/두 번째 난도만 사용하고 이후 두 번째 난도 반복. B01~B04와 발사 순간 조준탄 160/240/340. 첫 난도는 느린 조준탄만, 이후 실제 조준 발사마다 3속도 순환. 전체 시퀀스 예약 예산 적용 | `src/level.js`, `src/barrage.js`, `updateAttack` |
| 보급 | 첫 6초·이후 15초 간격, 3초 예고·2.1초 획득 가능. 상하 변경/유지 독립 획득. 기본 MAX 유지=드론 15초, 24초 회복·36초 이후 표식 드론. 옛 무작위/순환 드롭 대체 | `updateSupply`, `updatePickups`, `applyPickup` |
| 텐션 | 근접 여유 12, 새 탄당 1회·2초로 갱신, 피격 해제·무적 중 보상 없음. 발사 시 피해 1.5배·황금색·명중 큐를 탄에 고정 | `updateProjectiles`, `playerShot` |
| 전투 HUD | 기존 HP/점수·무기/드론에 사이클·기본 레벨·텐션·상하 바이저 예고 연결. 상단 앰버/하단 민트 고정, 실제 게임 사각형 기준 크기 대응 | `src/main.js`, `src/presentation.js` |
| 파일럿 HUD | 정상·blink·강화·저체력·피격. 피격 > 저체력 > 강화 > 정상. HP 25% 이하 경고, 실제 시간 기반 효과 | `src/pilot-state.js`, `src/pilot-ui.js` |
| 파일럿 FX3 | 피격 0.76초, 두 번의 점멸, 독립 적/청색 색층, 내부 이미지 반동 최대 10×6px. 프레임·마스크·기존 HP 위치 고정 | [파일럿 효과 기록](docs/pilot-ui/FX_REVISION.md) |
| 배경 | 기존 숲·구름 밀도/형상을 유지하며 하늘·구름·지형 낮밤 조명 연결. 일반전 1→1.5배·보스 3배·격파 후 1배 복귀, 누적 거리 연속 유지 | `src/volume-environment.js`, `src/terrain3d.js`, `updatePhase` |
| 음악 | 타이틀·일반·위급·보스·강화·게임오버 6곡. 상태별 우선순위와 교차 전환 | `assets/audio/manifest.json`, `chooseMusic`, `src/audio.js` |
| 효과음 | 기존 50샘플에 텐션 발사/명중·체인 4단계·보급 효과별 짧은 합성 공명 연결. 고정 피격 우선, 중복·보이스 상한 및 종료 정리 유지. 직접 청음 승인은 별도 | `src/audio.js`, `tests/audio.test.js` |
| 저장 | 새 점수 `skywind.best.v2`, 기존 v1 보존, 음소거 `skywind.muted.v1`. 재도전 새 시드. 기기·접속 주소 간 자동 동기화 없음 | `src/main.js` |

## 코드와 자산의 위치

- `src/game.js`: 시드 난수, 전투 시간, 이동/카메라 좌표, 적·탄·보스·아이템·점수·HP.
- `src/level.js`, `src/barrage.js`: 첫/두 번째 시험값·기본 성장·4패턴 시퀀스와 상대 이동 근접 거리.
- `src/main.js`: 입력, 화면 상태, 게임 프레임, 이벤트 연결, HUD, 음악 선택, 브라우저 저장.
- `src/renderer.js`: 전투 2D 렌더링, 기체 시퀀스, 구름 앞뒤 합성, 광원·효과.
- `src/volume-environment.js`, `src/terrain3d.js`: WebGL 2 배경. 추가 3D 엔진 의존성 없음.
- `src/pilot-state.js`, `src/pilot-ui.js`, `style.css`: 초상 상태·시간과 DOM 표현.
- `assets/art/player/sv01/`: 실행 매니페스트·WebP 21장, 보존 PNG·제작 매니페스트·미리보기.
- `assets/art/pilot/source/`: 제공 원본·참고 PNG 9개. 실행 초상은 `src/pilot-ui.js`의 이미지 목록을 따른다.
- `assets/audio/`: 음악 매니페스트, 현재·이전 음악, 생성 원본. 효과음 원본과 편집본은 각각의 source/edited 및 provenance로 구분.
- `tests/`: 전투·난이도·비행/무기·피격점·오디오·SV-01·파일럿 상태/DOM 테스트.
- `tools/build-site.mjs`: 명시된 실행 파일만 `dist/`로 생성. 새 실행 모듈·자산은 허용 목록과 로딩을 함께 갱신해야 한다.

## 로컬 실행과 동기화

Node.js와 WebGL 2/하드웨어 가속 브라우저가 필요하다. 현재 런타임 npm 의존성이 없어 `npm install`은 필요 없다.

```sh
npm start
```

기본 주소는 <http://127.0.0.1:5173/>. `file://` 실행은 지원하지 않는다. 포트 변경은 PowerShell에서 `$env:PORT = '5174'`처럼 설정한다. 실행 전에 실제 포트와 서버 프로세스를 확인하고 다른 작업의 서버를 종료하지 않는다.

Git 동기화·커밋·푸시·배포 범위는 AGENTS와 현재 사용자 승인을 따른다. 작업 전후 소스가 달라졌다면 오래된 검증 결과를 새 소스에 그대로 적용하지 않는다.

## 남은 검토와 설계 대상

- 우선 검토: 이번 로컬 확인본의 전투 감각·상하 이동·첫/두 번째 난도·HUD. 3단계 이후의 최종 난도 상승·극한 생존성과 신규 콘텐츠는 아직 구현하지 않았다.
- 플레이 감각: 작은 핵·텐션·발사 순간 조준·4패턴은 위 구현 표를 따른다. 자동 검사·통제 장면과 사람의 재미/공정함 승인을 구분한다. 실제 관측과 남은 제한은 [1차 구현 결과](docs/first-implementation/README.md)에 기록한다.
- 새 연출 방향: 기존 음원과 짧은 합성음을 사용하는 사건별 정상/텐션·체인 반응을 연결했다. 전체 [빛·소리 설계](docs/plan/AUDIO_VISUAL.md)의 최종 음색·새 자산 제작까지 완료한 것은 아니다.
- 효과음 제작: [제작·검토 도구](docs/plan/SFX_WORKBENCH.md)에 Node 합성·비교 화면과 자율 오디오 평가 경로를 제안했다. 사용자는 최초 후보를 직접 고르는 안보다 완전 자율 청음 방법의 추가 조사를 선택했다. Gemini·Qwen의 공식 오디오 입력 기능은 확인했으나 계정 연결·로컬 모델 실행·반복 개선 품질은 미실증이다. 새 도구·음원·API 호출·설치는 없으며 평가기와 채택 조건은 R17에서 검토한다.
- 메카닉 제작: 기존 곤충형 적·보스 외형을 공통 DNA의 드론·중간급·보스로 교체하고 Blender로 모델링·애니메이션·렌더할 방향은 확정했다. 현재 Blender 연결과 설치를 확인했으며 다른 작업의 열린 씬은 수정하지 않았다. 새 모델·렌더는 아직 없다. [메카닉 설계](docs/plan/MECHA_ART.md)의 역할·종수·약점 수치는 미결이다.
- UI: 기존 크기 대응 부족 피드백에 따라 실제 게임 영역 기준 배치를 적용했다. 이번 브라우저 크기 검증은 새 버전의 모든 실기 검증을 대신하지 않는다.
- 시각: 구름 근접면의 큰 면·부드러움 등 후속 아트 조정. 원본 참고 영상과 동일한 최종 품질로 승인된 것은 아니다.
- 청음: 최종 혼합 음질·DRONE 변주의 직접 청음 승인 기록은 없음. 기존 RMS 수치만으로 재정규화하지 않는다.
- 성능: 사용자 테스트가 있었으며, 기기별 수치·새 버전의 부하·장시간 안정성은 별도 확인이 필요하다.
- 과거 통합 QA ZIP은 미완성 기록이다. 새 개선의 완료 조건을 대신하지 않으며 자동으로 재제작하지 않는다.

이전에 보류했던 무기 교체·시간 연장·고도별 보급은 D42 승인 범위에서 이번 확인본에 구현했다. 근접 자동 흡수는 이후 D45 요청에 따라 R4로 추가했다. 전체 설계의 남은 제작을 자동으로 시작하지 않는다.

## 공개 사용과 역사적 증거

- 제공 사운드의 공개 사용 권한은 2026-09-07 사용자가 확인했다. [권한 기록](docs/RIGHTS_STATUS.md)과 provenance가 근거다. 새 자산의 권한이나 직접 청음 승인으로 확대하지 않는다.
- [최신 공개 배포](docs/PUBLIC_UPDATE_PILOT_20260908.md), [이전 SV-01 배포](docs/PUBLIC_UPDATE_SV01_20260907.md), [이전 PATCH2 배포](docs/PUBLIC_UPDATE_20260907.md)는 서로 다른 시점의 기록이다.
- [PATCH2 상태](docs/patch2/STATUS.md), [전후 스크린샷](docs/patch2/SCREENSHOTS.md), [당시 실행 지문](docs/patch2/evidence/runtime-version.json)의 78개 테스트·72개 파일은 과거 기준이다.
- [파일럿 최초 구현](docs/pilot-ui/README.md)과 [FX 개정](docs/pilot-ui/FX_REVISION.md)의 초기 수치·배포 전 표현은 해당 시점의 설명이다. 현재 소스와 최신 배포 기록을 우선한다.
- `docs/references/`의 사용자 제공 설계와 리뷰, V1/V2/V3/PATCH1 보고서, 참고 영상 분석은 역사적 근거로 보존한다. 그 안의 당시 작업 지시를 새 작업 명령으로 자동 적용하지 않는다.

## 저장소에 없는 원래 작업 자료

`.work/`, 기기별 `Temp/`, 대용량 원시 QA 녹화·이전 QA ZIP·임시 터널은 백업 대상에서 제외됐다. 참고 원본 `SampleGame.mp4`도 공개 저장소에 없다. 이 자료가 실제로 필요한 비교에서만 위치와 접근 가능 여부를 확인한다.

현재 SV-01 폴더에는 렌더 PNG·실행 WebP·제작 매니페스트와 원본 Blender 해시가 있지만, 이번 저장소 파일 조사에서 해당 SV-01 .blend는 찾지 못했다. 해시만으로 원본 카메라·재질 설정을 복원할 수 있다고 가정하지 않는다. 제작 시 원본 위치를 확인하거나 렌더 기준을 새로 맞춰야 한다.

`assets/audio/source/`의 음악 생성 MP4와 Blender·PNG 등 제작 원본은 임시 QA 녹화와 다르며 보존·백업 대상이다. 게임 실행을 위해 과거 원시 QA 자료를 재제작할 필요는 없다.
