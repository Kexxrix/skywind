# PATCH2 공개 업데이트 — 2026-09-07

사용자의 'GitHub 업로드가 끝나면 공개사이트를 최신상태로 적용' 지시에 따라, 최초 GitHub 백업 완료 후 **기존 공개 사이트에 버전 3**을 배포했다. 공개 주소와 공개 범위는 유지했다.

- URL: <https://skywind.kexxadrix.chatgpt.site/>
- Sites 상태: **succeeded**, 2026-09-07 **10:03:34 KST**.
- 게임: `SKYWIND-PATCH2-LOCAL-20260906-R1`.
- 런타임 지문: `5f7d69a37a71d12479cf8bd599905ec912da63998fd66628f980581525644b63`.
- 최초 GitHub 백업 커밋: `84799b80ae9667ce292827716cc8deccc41425f8`.
- Sites 소스 커밋: `1c33a5a02c719afd0a26b174cc36ce09f7c57af0`.
- 버전 ID: `appgprj_6a9c5fa064288191a799816ed259f986~appgver_36298e7e254881918cf691ef82e333d0`.
- 배포 ID: `appgdep_6a9e0d5a14f08191aada4add4c2c2fe1`.

## 적용 내용

구름의 중간 형태·깊이·가로줄 완화, 플레어를 보존한 근경 구름 색 번짐 완화, 최저 고도에서 수목이 전투 레이어를 덮던 문제, 반경 **1.85**의 중앙 핵 표시와 연속 피해 판정, 사운드 정지·음소거·게임오버·재시작 종료 처리를 적용했다. 이전 공개본에 없던 PATCH1 적탄 C안·무기/HUD 공통 색과 제공 효과음 편집본 30개도 함께 반영한다.

게임 규칙·무기 시간/교체/드롭·음악·지상 모델은 검증한 PATCH2 그대로다. 새로운 아이템 시스템이나 보상적 적 난이도 상향은 적용하지 않았다. 게임 코드나 실행 자산은 GitHub 백업 후 추가 수정하지 않았다.

## 검증

| 항목 | 결과 |
|---|---|
| GitHub 최초 백업 | 로컬 HEAD / origin/main / 실제 원격 main 일치. HANDOFF.md 익명 HTTP 200 |
| Sites 배포용 별도 소스 사본 | 게임 런타임 72개가 GitHub 백업과 SHA 동일. 테스트 **78/78**, 문법 검사 통과 |
| 정적 빌드 / 패키지 | 실행 파일 72개 + 호스팅 메타데이터 1개. 커밋·패키지의 런타임 해시 일치 |
| Sites 배포 | 기존 public 접근 유지, 버전 3 성공 상태 확인 |
| 익명 공개 HTTP | `/` 및 실행 파일 72개 모두 200. 쿠키·인증 헤더 없이 확인 |
| 공개 파일 무결성 | JS/CSS/이미지/오디오 등 **71개는 원본과 바이트 동일**. `index.html`에는 Cloudflare가 challenge-platform 스크립트를 추가하며, 관측한 그 삽입문 하나를 제외하면 앱 HTML도 정확히 동일 |

공개 파일별 결과: [PUBLIC_PATCH2_VERIFICATION_20260907.json](PUBLIC_PATCH2_VERIFICATION_20260907.json). JSON의 `matched: 71`은 원시 바이트 일치 수다. HTML은 `indexVerification`의 별도 검사에 통과했으며 `verificationPassed: true`다. '72개 원시 바이트 전부 동일'이라고 기록하지 않는다.

배포는 이전 변경 중이던 `.work/sites-source`를 덮어쓰지 않고, 같은 Sites 원격의 새 별도 사본에서 기존 이력을 이어서 수행했다. 실행 파일만 패키지에 포함했으며 제작 원본·QA 문서·영상·로그·인증정보는 게임 공개 산출물에 넣지 않았다. 패치 사운드는 2026-09-07 사용자의 공개 사용 권한 확인을 근거로 포함했다.

## 남은 제한

이 배포 작업에서는 브라우저 화면·게임 조작·실제 청음을 새로 검증하거나 녹화하지 않았다. 공개 서버에서 제공하는 정확한 실행 파일과 기존 로컬 테스트를 확인했다. 게임의 아트 승인·직접 청음·미완료 통합 QA 상태는 [PATCH2 상태](patch2/STATUS.md)를 따른다. 기존에 열어 둔 브라우저는 새로고침해야 새 코드가 실행된다.
