# SkyWind 공개 업데이트 확인

- 공개 주소: [SkyWind](https://skywind.kexxadrix.chatgpt.site/)
- 배포 완료: 2026-09-06 18:21:20 KST
- Sites 버전: 2
- 배포용 소스: `9eea542c899cc56ec8b752d922f14185af28fadb`
- QA 제출 버전과 동일한 런타임 SHA-256 집합: `606be211af486fb77cbabc411cb5e529cd58e8a23d69a314bbdcb6b0f2017be1`

## 확인된 문제와 반영 내용

공개 사이트에는 첫 배포 버전이 남아 있었다. 최신 로컬과 비교하면 `index.html`, `style.css`, `src/game.js`, `src/main.js`, `src/audio.js`, `src/renderer.js`, `src/volume-environment.js`의 7개 파일이 달랐다. 첫 보스가 38초에 등장하는 이전 난이도와 새 무기 HUD가 없는 상태였다.

기존 공개 주소에 QA ZIP을 만들 때 검증한 최신 버전을 배포했다. 확장된 상하 이동과 카메라 추적, 구름 형태·광원 조정, 구간별 전투 압력 제한과 첫 보스 120초, 무기 아이콘·남은 시간·게이지·독립 드론 표시, 무기별 발사·명중 및 경고·만료 음향 처리가 포함된다. 이번 배포를 위해 게임 규칙이나 런타임 코드를 추가 수정하지 않았다.

## 검증

- 테스트 53/53 통과, JavaScript 문법 검사 통과, 정적 빌드 41개 파일 생성 성공.
- 로컬·Sites 전용 체크아웃·커밋·빌드·배포 아카이브의 런타임 41개 파일 SHA-256 일치.
- Sites 배포 상태 `succeeded` 확인.
- 배포 후 익명 공개 HTTPS 접속에서 41/41 파일 최신 버전 일치. 변경된 기본 URL 7개도 쿼리 없이 다시 확인했다.
- HTML은 Cloudflare가 요청마다 삽입하는 challenge 스크립트만 제거한 뒤 비교했다. 게임 본문과 나머지 파일은 최신 로컬과 일치한다.
- 공개 사이트를 새 Chrome 세션에서 실행해 로딩 완료, START, 실제 게임 시간 진행, 사격, 상단 이동과 카메라 약 -156, TWIN CANNON 아이콘·무제한 표시를 확인했다. 음악과 효과음 로딩 상태가 준비 완료였고 페이지 오류와 HTTP 오류는 없었다.
- 기존 인앱 공개 탭도 새로 열어 로딩이 끝나고 START 화면이 표시되는 것을 확인했다.

## 범위

기존 공개 접근 권한을 유지했다. QA 보고서·ZIP·참고 영상·제작 원본·내부 작업 파일은 게임 배포에 포함하지 않았다. 기존 개발 서버와 QA 공유 터널은 변경하지 않았다.

공개 배포 확인은 짧은 실행 검사다. 5분 이후 플레이와 무기별 상세 영상은 동일한 런타임 버전으로 제작한 QA ZIP의 검증 자료를 사용한다. 실제 태블릿 성능, 구름의 최종 미감, 사람이 느끼는 난이도와 효과음 선호까지 새로 검증한 것은 아니다.
