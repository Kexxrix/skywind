# PATCH2 인계용 상태 — 2026-09-07

대상은 `SKYWIND-PATCH2-LOCAL-20260906-R1`이다. [실행 파일 지문](evidence/runtime-version.json)은 2026-09-06 22:22 KST에 확정되었으며 최초 GitHub 백업 때 같은 72개 파일임을 다시 검사한다. 이 문서는 기존 증거를 인계하는 상태 문서이며, 미완료였던 통합 QA를 완료로 바꾸지 않는다.

| 요청 | 실제 구현 / 주요 수치 | 상태 / 증거 |
|---|---|---|
| 구름 중간 형태·깊이·가로줄 | 큰 4×4 배치 보존, 측면 6→10·왕관 4→7 돌출, 낮은 무리 10개 돌출, 약 26 단위 중간 요철. 128회 한도 내 구름 진입부 최대 1.2 단위 적응 샘플링 | 구현·일부 화면 검증. 근접 큰 면/부드러움 아트 조정은 남음. `before/after-cloud-*.png`, [구름 기록](cloud-notes.md) |
| 플레어 유지, 근경 색 번짐 완화 | 광원별 공간 반경·감쇠, 합산 추가 RGB 상한 .72. 실제 2D 플레어는 보존 | 비교 검증. 고정 장면 최대 변화 면적: 사격 85.97→2.27%, 폭발 88.81→11.06%. [측정](evidence/light-results.json), `before/after-light-*.png` |
| 최저 고도 수목의 기체·탄 가림 | 지형은 뒤에 합성하고 전경 합성은 구름만 출력. 지상 자산·모델은 보존 | 실제 화면과 이동 관측 있음. `before/after-low-occlusion.png`, `after-low-input.png`. 타 GPU/화면 조건 미검증 |
| 적탄 C·무기 색/HUD·기존 규칙 유지 | 기존 표현·색·시간·교체·드롭 유지. 25개 보호 함수/블록 동일 | [보존 검사](evidence/gameplay-preservation.json), `after-weapon-*.png` |
| 사운드 종료·예외 | pause/mute 잔류 샘플·예약 음표 정리, gameover 피격/파괴 보존, resetEffects로 재시작/타이틀 경계 초기화, 비동기 suspend/resume 경합 수정 | 오디오 모의 테스트 17/17. [오디오 기록](audio-notes.md). 실제 최종 혼합 청음 미검증 |
| 핵 표시와 피해 판정 일치 | 공유 반경 17→1.85. 밝은 핵만 피해 판정, 대비 받침 3.35/날개/광번짐 제외. 최대 1/120초 내부 스텝에서 상대 이동 스윕 | 핵·날개·고속·프레임·카메라 자동 테스트. `after-wing-graze*`, `after-core-hit*`, `after-fast-core*`; [판정 기록](hitpoint-notes.md) |
| 모든 피해 경로 / 획득 편의 | 적탄·지뢰와 원/타원 몸통 확인. 실제 연속 레이저 피해 경로 없음(긴 선은 예고). 획득 여유 31, 보통 아이템 총 53 유지 | 전체 테스트 78/78 기록. 회전 타원은 기존 확장 타원의 작은 스텝 근사이며 임의의 급회전까지 정확한 해석해는 아님 |
| 실제 난이도 변화 | 20시드×120초, 고정 비반응형 입력: 생존 4/20→18/20, 평균 HP 3.9→38.2 | [측정](evidence/hitpoint-impact.json). 이 입력에서 쉬워짐. 인간 체감·후반 보스 적정성 미검증. 보상 상향 없음 |

## 보존한 자료와 해석 주의

- [화면 목록](SCREENSHOTS.md): 실제 게임 렌더러로 만든 전후 PNG 40장. 전후 구름/광원 비교는 통제된 장면과 테스트 설정이며 일반 생존 플레이로 표시하지 않는다.
- `evidence/before-scenes.json`, `after-scenes.json`은 원래 캡처 로그다. after 로그의 `variant: baseline`은 캡처 도구에 남은 기본 라벨이다. 현재 반경 1.85와 runtime 지문, after 파일명으로 대상을 식별하며 문자열만으로 이전 소스로 판단하지 않는다. 원본 증거 자체는 고치지 않았다.
- [기존 무기 비교 MP4](videos/01_weapon_comparison.mp4)는 2026-09-06 생성된 기존 파일을 그대로 보존했다. 테스트 기능을 사용한 비교 영상이다. 이번 백업에서는 새로 녹화하거나 편집하지 않았고, 이 한 편으로 전체 QA나 최종 음질 검증을 완료했다고 주장하지 않는다.
- 이전 실제 녹화 원시 자료는 전투 약81초 / 이동 약52초 / 구름 약26초였으며, 장시간 작업 정체를 사용자가 중단했다. 최종 이동 재녹화는 불완전했고 최종 구름 재녹화는 생성되지 않았다. 원시 JPG/오디오와 중간 인코딩은 저장소에서 제외했다.
- [기존 AV 분석](evidence/flight-av.json)은 원시 이동 녹화의 시간/파형 분석이다. 최종 이동 MP4의 검증 결과가 아니다. 초기 고속 주입 피격 한 사례에 AudioContext 재개 경합으로 음원 큐가 누락됐고 보완 촬영은 미완료다.
- 각 분야 notes는 당시 하위 작업 기록이라 'root 확인 필요'나 당시 중간 테스트 실패를 포함한다. 최종 전체 자동 테스트는 78/78로 해결됐지만 직접 청음·장시간 성능·최종 통합 미디어/ZIP까지 완료된 것은 아니다.
- `.work/`, 외부 ZIP 및 진단 도구를 가리키는 과거 경로는 출처 기록이다. 이 폴더에 복사된 증거 외에 과거 작업 전체가 이 저장소에 들어 있다는 뜻은 아니다.

## 다음 검토

영상 대신 핵 스침/직격·최저 고도·구름 전경의 짧은 실제 화면 관측과 기존 테스트를 사용한다. 필요 시 사용자 청음으로 DRONE 변주와 종료 상태를 확인한다. 새로운 아이템 시스템·적 보상 상향은 시작하지 않는다. 기존 QA ZIP을 미완료로 남겼다는 사실을 숨기지 않는다.
